<div class="card w-75 my-3">
    <div class="card-title d-flex justify-content-between px-2">
        <h3>{{post.title}}</h3>
        <h3>Posted by {{post.user.username}} at {{created_at}}</h4>
    </div>
    <div class="card-body">{{post.content}}</div>
</div>
<h3>Comments</h3>
{{#each post.comments as |comment|}}
{{> comment-details}}
{{/each}}